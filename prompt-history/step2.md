okay great. Now let's move on to the next step. The next step is generating the first frame of the animation using an external image generation model. So this is going to be the first frame of the animation in the video model, then animate from this thing. We'll generate the prompt of the video model later, but right now we're focusing entirely on generating the first frame of the animation. So what I want you to do is come up with a highly detailed and descriptive prompt for the image model. You should include all the stylistic factors that you need to include because the image model really has zero context on any style, so include - be very descriptive: the style, be very descriptive, everything extremely descriptive - fonts, positions, colors, grain, texture, etc., just be hyper, hyper, hyper, hyper descriptive. also please note: the image model that I'm working with can't output a true 9:16, unfortunately. It can output an image that's basically a 2:3 aspect ratio. I need to basically crop it into a 9:16, but that means I end up cutting off about 10% width on each side (vertical stays untouched). So basically, I want to make sure that any image regenerate has like the core thing that matters, there's like an ample amount of kind of padding on the left and right side and the top and bottom. So, like really, you should kind of think of it like everything should be within about, say, 60% width of basically the frame, because we're going to be reducing the width by about 20% total and still want to have padding leftover so its framed nicely. only output the image prompt nothing else, no acknlowedgemnet, preface explanaiton etc.